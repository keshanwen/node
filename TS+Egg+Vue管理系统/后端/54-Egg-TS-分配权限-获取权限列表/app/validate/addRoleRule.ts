export default {
    roleName: {
        type: 'string',
        trim: true,
        allowNull: false
    },
    roleDesc: {
        type: 'string',
        trim: true,
        allowNull: false
    }
}
